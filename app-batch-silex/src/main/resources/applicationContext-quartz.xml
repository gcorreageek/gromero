<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:jee="http://www.springframework.org/schema/jee"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
    	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
    	http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.0.xsd">

	<bean id="jobSilexDetail" class="org.springframework.scheduling.quartz.JobDetailFactoryBean">
	  <property name="jobClass" value="pe.com.silex.batch.SilexJob"/><!-- Clase donde se ejecutara la logica -->
	  <property name="durability" value="true"/>
	</bean>
	<!-- <bean id="silexTrigger" class="pe.com.silex.batch.PersistableCronTriggerFactoryBean">
	  <property name="jobDetail" ref="jobSilexDetail" />
	  run every minute at 5:00 PM
	  <property name="cronExpression" value="0 * 17 * * ?" />
	</bean> -->
	
	<bean id="silexTrigger" class="pe.com.silex.batch.PersistableSimpleTriggerFactoryBean"><!-- Clase que setea el valor de cada cuanto tiempo se ejecuta -->
	  <property name="jobDetail" ref="jobSilexDetail" />
	  <!-- Parametrizado en clase desde la BD, valor por defecto de 7200000(2Horas) si ocurre un error en la consulta a BD -->
	  <property name="repeatInterval" value="7200000" />
  	  <property name="startDelay" value="20000" />
	</bean>
	 
	<bean id="quartzSchedulerSilex" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		 <property name="configLocation" value="classpath:quartz.properties"/>
		 <property name="dataSource" ref="dataSource"/>
		 <property name="transactionManager" ref="transactionManager"/>
		 
		 <!-- This name is persisted as SCHED_NAME in db. for local testing could change to unique name to avoid collision with dev server -->
		 <property name="schedulerName" value="quartzSchedulerSilex"/>
		 
		 <!-- Will update database cron triggers to what is in this jobs file on each deploy. Replaces all previous trigger and job data that was in the database. YMMV  -->
		 <property name="overwriteExistingJobs" value="true"/>
		 
		 <property name="autoStartup" value="true"/>
		 <property name="applicationContextSchedulerContextKey" value="applicationContext"/>
		 <property name="jobFactory"> <bean class="pe.com.silex.batch.AutowiringSpringBeanJobFactory"/> </property>
		 
		 <!-- NOTE: Must add both the jobDetail and trigger to the scheduler! -->
		 <property name="jobDetails"> <list> <ref bean="jobSilexDetail" /> </list> </property>
		 <property name="triggers"> <list> <ref bean="silexTrigger"/> </list> </property>
	</bean>
</beans>